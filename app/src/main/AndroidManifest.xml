<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="io.mangel.issuemanager">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
            android:name=".activities.IssueManagerApplication"
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity
                android:name=".activities.maps.MapListActivity"
                android:label="@string/title_map_list"
                android:parentActivityName=".activities.overview.OverviewActivity"
                android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                    android:name="android.support.PARENT_ACTIVITY"
                    android:value="io.mangel.issuemanager.activities.overview.OverviewActivity"/>
        </activity>
        <activity
                android:name=".activities.maps.MapDetailActivity"
                android:label="@string/title_map_detail"
                android:parentActivityName=".activities.maps.MapListActivity"
                android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                    android:name="android.support.PARENT_ACTIVITY"
                    android:value="io.mangel.issuemanager.activities.maps.MapListActivity"/>
        </activity>
        <activity android:name=".activities.overview.OverviewActivity">
        </activity>
        <activity
                android:name=".activities.login.LoginActivity"
                android:label="@string/app_name"
                android:noHistory="true"
                android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>